# Example env file (SAFE placeholders only)
# Put REAL values in a local .env file (which must be gitignored)

export MLFLOW_BACKEND_STORE_URI="postgresql://USER:PASSWORD@HOST/mlflowdb?sslmode=require"
export MLFLOW_DEFAULT_ARTIFACT_ROOT="s3://your-bucket/mlflow"

export AWS_ACCESS_KEY_ID="YOUR_AWS_ACCESS_KEY_ID"
export AWS_SECRET_ACCESS_KEY="YOUR_AWS_SECRET_ACCESS_KEY"
export AWS_DEFAULT_REGION="us-east-1"

# export MLFLOW_TRACKING_URI="http://YOUR-EC2-PUBLIC-IP:5000"
